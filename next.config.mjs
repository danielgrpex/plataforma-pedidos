/** @type {import('next').NextConfig} */
const nextConfig = {
  // 🔹 Copia aquí TODO lo que tienes dentro del objeto en next.config.ts
  // por ejemplo:
  // experimental: {},
  // output: "standalone",
};

export default nextConfig;
